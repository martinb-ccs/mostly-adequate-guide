//========================================================
// require
//========================================================
require('../diagnostics');

//========================================================
// functions
//========================================================
const add = x => y => x + y;
const increment = add(1);
const addTen = add(10);

increment(2).log(); // 3
addTen(2).log();    // 12